SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# Variables d'environnement
TARGET_HOST=172.17.0.1
TARGET_USER=root
SSH_KEY_PATH=/root/.ssh/id_rsa

# Exécuter l'audit toutes les minutes
* * * * * /app/audit.rb -o json -c 0.1 -m 0.1 -p /output/audit_$(hostname).json >> /var/log/audit-cron.log 2>&1
